# Base URL the frontend uses to reach the backend API tier.
# In docker-compose this is proxied through Nginx (see nginx.conf), so it
# can be left as a relative path. Set it to an absolute URL if the backend
# is exposed on a different host/port (e.g. local `npm run dev`).
VITE_BACKEND_URL=http://localhost:8000
